{
  "name": "betpawa-cli",
  "version": "0.1.0",
  "description": "Read-only BetPawa Uganda CLI for games, fixtures, odds, and live results.",
  "type": "module",
  "bin": {
    "betpawa": "./bin/betpawa.js"
  },
  "scripts": {
    "start": "node ./bin/betpawa.js",
    "check": "node --check ./bin/betpawa.js && node --check ./src/api.js && node --check ./src/cache.js && node --check ./src/open-data.js && node --check ./src/stats.js && node --check ./src/predict.js && node --check ./src/format.js && node --check ./src/cli.js",
    "test": "node --test"
  },
  "keywords": [
    "betpawa",
    "sportsbook",
    "odds",
    "fixtures",
    "cli",
    "uganda"
  ],
  "author": "",
  "license": "MIT",
  "optionalDependencies": {
    "playwright": "^1.56.1"
  },
  "engines": {
    "node": ">=18"
  }
}
